<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <p>
      Install the new build at <%= AppConfig.host + app_build_path(@build.app, :build_id => @build.id) %>
    </p>

    <br />

    <p>
      <h2>What's new:</h2>
      <%= simple_format @build.release_notes %>
    </p>
  </body>
</html>